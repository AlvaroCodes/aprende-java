# Ejercicio: ValidarUsuario

Declara una variable `usuario` (String). Comprueba: no esté vacía, tenga al menos 5 caracteres, no contenga espacios y esté en minúsculas. Imprime mensajes para cada fallo y, si todo pasa, imprime "Usuario válido".
